<h1>TV Discovery App</h1>
<h4>Popular Shows</h1>
<section class="popular-shows-list">
<ol>
<%  @popular_shows.each do |show| %>
  <li><%= show.name %></li>
<% end %>
</ol>
</section>

<%= form_tag(shows_path, :method => "get", class: "form") do %>
  <div class='form-group' aria-hidden='true'>
  <%= text_field_tag(:name, params[:name], placeholder: "Search TV Shows") %>
  </div>
  <div class='form-group' aria-hidden='true'>
  <%= submit_tag("Search", class: "btn btn-success") %>
  </div>
<% end %>
